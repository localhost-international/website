{
  "name": "localhost",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "develop": "nodemon --watch '*.ts' --exec 'ts-node' index.ts",
    "build": "tsc -p .",
    "deploy": "gh-pages -d dist",
    "deploy:build": "npm run build && gh-pages -d dist",
    "test": "echo \"There currently aren't any tests.\"",
    "lint": "echo \"Currently not linting code\""
  },
  "dependencies": {
    "gh-pages": "^2.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.5"
  }
}
